<%= form_for(@erabat) do |f| %>
  <% if @erabat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@erabat.errors.count, "error") %> prohibited this erabat from being saved:</h2>

      <ul>
      <% @erabat.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
